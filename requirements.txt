# Dependencias del repo (modo simulación — lo que corre el CI sin equipos).
jinja2>=3.1
pyyaml>=6.0

# --- Modo real (contra Central / PAN-OS), opcional ---
# Descomentar al apuntar a infraestructura real:
# pycentral>=1.4              # API de Aruba Central (claim, config, backup)
# pan-os-python>=1.12         # API de PAN-OS (política, backup XML)
